{
  "name": "my-capabilities",
  "version": "0.0.1",
  "private": true,
  "description": "my-capabilities",
  "license": "OGL",
  "repository": {
    "type": "git",
    "url": "git://github.com/jncc/my-capabilities.git"
  },
  "author": "",
  "keywords": [
    ""
  ],
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "lint": "tslint --force --format verbose \"src/**/*.ts\"",
    "build": "npm run clean && npm run lint && echo Using TypeScript && tsc --version && tsc --pretty",
    "build:watch": "npm run build -- --watch",
    "test": "npm run build && mocha --compilers ts:ts-node/register --recursive test/**/*.ts",
    "test:watch": "npm run test -- --watch"
  },
  "dependencies": {
    "express": "^4.14.0"
  },
  "devDependencies": {
    "@types/chai": "3.4.30",
    "@types/chai-string": "^1.1.30",
    "@types/express": "^4.0.33",
    "@types/mocha": "2.2.29",
    "@types/node": "6.0.31",
    "chai": "^3.5.0",
    "chai-string": "^1.2.0",
    "mocha": "^3.0.1",
    "rimraf": "^2.5.4",
    "ts-node": "^1.2.2",
    "tslint": "3.15.1",
    "typescript": "2.0.3"
  },
  "engines": {
    "node": ">=4.0.0"
  }
}
